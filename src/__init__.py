"""Reusable modules for the LendingClub probability-of-default model.

The notebook in ``01_notebooks/`` keeps the exploratory analysis and the
plots; every processing, training and evaluation step lives here so it can be
imported and tested without a notebook kernel.
"""
